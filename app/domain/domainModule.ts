module app.domain {
	'use strict';

	// Create the module and define its dependencies.
	angular.module('domain', [
		// Angular modules

		// Custom modules
		'dataAccess'
		
		// 3rd Party Modules
	]);
}